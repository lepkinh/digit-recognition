// about page component

import React from 'react';

const About = () => {
    return (
        <div>
            <h1>About This Project</h1>
            <p>This is a digit recognition project using a neural network trained on the MNIST dataset. etc etc etc add info here....</p>
        </div>
    );
};

export default About;
